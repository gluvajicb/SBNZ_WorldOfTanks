package KieRule;
import com.example.WoTRecommender.model.Tank;
import com.example.WoTRecommender.model.Tank.TankType;



rule "changes tank type if condition is fulfilled"
 when
    tankObject : Tank(damage == 750 && topSpeed == 25)
 then
    tankObject.setTankType(Tank.TankType.DESTROYER);
 end